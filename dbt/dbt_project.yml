name: 'weather_analytics'
version: '1.0.0'
config-version: 2

profile: 'weather_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_analytics:
    materialized: table
    staging:
      materialized: view
      schema: staging
    mart:
      materialized: table
      schema: mart